[env:esp32]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600
; CAN uses the built-in ESP-IDF TWAI driver — no external library needed
; For UART bridge mode: change monitor_speed to 921600 and uncomment USE_UART_BRIDGE in main.cpp
